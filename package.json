{
  "name": "solicitud-lona-especializada-yaavs",
  "version": "1.0.0",
  "private": true,
  "description": "Solicitud de diseño y producción de lona especializada YAAVS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node -e \"console.log('No build step')\"",
    "deploy:gas": "npx @google/clasp push --force && npx @google/clasp deploy --description \"Deploy $(date '+%Y-%m-%d %H:%M')\"",
    "deploy:all": "bash scripts/deploy-all.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "multer": "^2.2.0"
  }
}
